//1 A Functions which prints Hello World

function printHelloWorld() {
	return "Hello World";
}
console.log( printHelloWorld() );
alert( printHelloWorld() );
document.write( printHelloWorld() );


//2 A Function which used to add two numbers.

function add (num1, num2) {
    var total = num1 + num2;
    return total;		
}
document.write(add(45, 100));
